name: UpdateTemp

on:
  schedule:
    - cron: '*/5 * * * *' 
env:
  TEMPTEXT: "TEST"
jobs:
  before_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test26
        run: |
          echo "VVVVVV $env_var"
      - name: Commit changes
        run: |
          git config user.name "temp bot"
          git config user.email "tempbot@ozwk.net"
          git add README.md
          git commit -m "Add AAAAA to README"
          git push
