name: UpdateTemp

on:
  schedule:
    - cron: '*/5 * * * *' 
jobs:
  before_job:
    runs-on: ubuntu-latest
    steps:
      - id: test0
        run: |
          readme_content=$(cat README.md)
          echo "$readme_content"
